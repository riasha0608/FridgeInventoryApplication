<!-- Basic skeleton generated by Emmet Abbreviation "!" in VS Code -->
<!DOCTYPE html>
<html lang="en" class="orange lighten-5">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fridge Inventory</title>

    <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Balsamiq+Sans:wght@700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&display=swap" rel="stylesheet">

    <!-- Materialize CSS and Icons-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

    <link rel="stylesheet" href="/stylesheets/detailsStyle.css">
    <link rel="stylesheet" href="/stylesheets/navBarStyle.css">

</head>

<body>

    <%- include('partials/navbar') %>

    <!-- Actual page content! -->
    <div class="container">
        <div class="section flow-text" id="stuffDetail">
            <h1>Food item details:</h1>
            <a class="btn-small waves-effect waves-light blue darken-4 white-text" href="/fridge">
                <i class="material-icons left">arrow_back</i>Return to Inventory
            </a>
            <table>
                <tr>
                    <th>Name:</th>
                    <td><%= fridge.food_item_name %></td>
                </tr>
                <tr>
                    <th>Category:</th>
                    <td><%= fridge.food_category_name %></td>
                </tr>
                <tr>
                    <th>Meal:</th>
                    <td><%= fridge.meal_name %></td>
                </tr>
                <tr>
                    <th>Expiration Date:</th>
                    <td><%= fridge.expiration_date_formatted %></td>
                </tr>
                <tr>
                    <th>Quantity and Unit:</th>
                    <td><%= fridge.quantity_and_unit %></td>
                </tr>
            </table>
        </div>

        <div class="section" id="stuffControls">
            <a class="btn-large waves-effect waves-light cyan left modal-trigger" href="#stuffEdit">
                <i class="material-icons right">edit</i>
                Edit
            </a>
            <a class="btn-large waves-effect waves-light indigo right" href="/fridge/<%= fridge.food_item_id %>/delete">
                <i class="material-icons right">delete</i>
                Delete
            </a>
        </div>

        <div class="modal purple lighten-5" id="stuffEdit">

            <div class="modal-content">

                <h2>Edit food item:</h2>
                <form id="updateForm" method="post" action="/fridge/<%= fridge.food_item_id%>">
                    <div class="row">
                        <div class="input-field col s6">
                            <label for="nameInput">Food item name:</label>
                            <input type="text" name="food_item_name" id="name" class="validate" data-length="32" value = "<%= fridge.food_item_name%>">
                        </div>
                        <div class="input-field col s6">
                            <select type = "number" id="categoryInput" name="category_id" required>
                                <option value="" disabled>Choose your food category</option>
                                <% for (let i = 0; i < foodCategoriesList.length; i++) { %>
                                    <option value=<%=foodCategoriesList[i].food_category_id %>
                                        <% if (fridge.category_id == foodCategoriesList[i].food_category_id) { %> selected <% } %> ><%= foodCategoriesList[i].food_category_name %></option>
                                <% } %>
                            </select>
                            <label for="categoryInput">Food category:</label>
                        </div>
                        <div class="input-field col s6">
                            <select type = "number" id="mealInput" name="meal_id">
                                <option value="" disabled>Choose your meal</option>
                                <% for (let i = 0; i < mealsList.length; i++) { %>
                                    <option value=<%=mealsList[i].meal_id %>
                                        <% if (fridge.meal_id == mealsList[i].meal_id) { %> selected <% } %> ><%= mealsList[i].meal_name %></option>
                                <% } %>
                            </select>
                            <label for="mealInput">Meal:</label>
                        </div>
                        <div class="input-field col s6">
                            <label for="expirationDate">Food Expiration Date:</label>
                            <input type="date" name="expiration_date" id="expirationDate" class="validate" data-length="32" value = "<%= fridge.expiration_date_YMD%>">
                        </div>
                        <div class="quantity_unit input-field col s6">
                            <label for="quantityInput">Food item quantity and unit:</label>
                            <input type="text" name="quantity_and_unit" id="quantity" value= "<%= fridge.quantity_and_unit%>">
                        </div>
                        
                        
                    </div>
                </form>
                <div class="modal-footer purple lighten-5">
                    <button class="btn waves-effect waves-light cyan" type="submit" form="updateForm">
                        <i class="material-icons right">send</i>
                        Submit
                    </button>
                    <button class="modal-close waves-effect waves-green btn-flat deep-purple white-text" type="cancel">
                        <i class="material-icons right">sentiment_neutral</i>
                        Never Mind
                    </button>
                </div>
            </div>
        </div>

        
        
    </div>

    <!-- Materialize JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

    <!-- initialize Materialize elements -->
    <script src="/scripts/materializeAutoInit.js"></script>

</body>

</html>